apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginxapp1-ingress-service
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          service:
            name: app1-nginx-deployment-service
            port:
              number: 80
